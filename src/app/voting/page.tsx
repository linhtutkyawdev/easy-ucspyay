// import { getUser } from "@/lib/utils";
// import VerifyInfo from "@/components/verify-info";

import Voting from "./client/carousel";

export default async function Page() {
  return <Voting />;
}

// just allow everyone to vote, but have temp users who will be discarded after like 30 days
